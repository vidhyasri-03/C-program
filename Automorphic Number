#include <math.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <assert.h>
#include <limits.h>
#include <stdbool.h>

int main() {
    int a;
    scanf("%d",&a);
    if(0<a && a<10)
    {
        if((a*a)%10==a)
        {
            printf("Automorphic Number");
        }
        else
        {
            printf("Not Automorphic Number");
        }
    }
    else
    {
        printf("Invalid Input");
    }
    
    return 0;
}
