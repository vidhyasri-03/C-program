#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int n;
    scanf("%d",&n);
    if(n%2==0)
        {
        printf("The given input %d is Even",n);
        }
    else
        {
        printf("The given input %d is Odd",n);
        }
    return 0;
}
